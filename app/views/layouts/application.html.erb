<!DOCTYPE html>
<html>
<head>
  <title>The Gossip Project</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body class="bg-light">

  <nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top">
    <div class="container">
      
      <a class="navbar-brand d-flex align-items-center fw-bold text-primary" href="/">
        <%# CORRECTION ICI : On utilise du style CSS pour fixer la hauteur et laisser la largeur auto %>
        <%= image_tag "logo_v2.png", alt: "Logo", class: "me-2", style: "height: 40px; width: auto;" %>
        GOSSIP
      </a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><%= link_to "Accueil", root_path, class: "nav-link text-dark" %></li>
          <li class="nav-item"><%= link_to "Les Potins", gossips_path, class: "nav-link text-dark" %></li>
          <li class="nav-item"><%= link_to "Team", team_path, class: "nav-link text-dark" %></li>
          <li class="nav-item"><%= link_to "Contact", contact_path, class: "nav-link text-dark" %></li>
          
          <% if logged_in? %>
            <li class="nav-item ms-lg-3">
              <%= link_to "Nouveau Potin", new_gossip_path, class: "btn btn-primary btn-sm px-3 fw-bold" %>
            </li>
            
            <li class="nav-item dropdown ms-lg-3">
              <a class="nav-link dropdown-toggle fw-bold text-dark" href="#" role="button" data-bs-toggle="dropdown">
                <%= current_user.first_name %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                <li><%= link_to "Mon Profil", user_path(current_user), class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <%= button_to "Déconnexion", session_path(current_user), method: :delete, class: "dropdown-item text-danger" %>
                </li>
              </ul>
            </li>

          <% else %>
            <li class="nav-item ms-lg-3">
              <%= link_to "Connexion", new_session_path, class: "nav-link fw-bold text-primary" %>
            </li>
            <li class="nav-item ms-lg-2">
              <%= link_to "S'inscrire", new_user_path, class: "btn btn-outline-primary btn-sm px-3 fw-bold" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <main>
    <div id="flash-container">
      <% flash.each do |type, message| %>
        <div class="container mt-4">
          <div class="alert alert-<%= type == 'success' ? 'success' : 'danger' %> alert-dismissible fade show shadow-sm border-0" role="alert">
            <%= message %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        </div>
      <% end %>
    </div>

    <%= yield %>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>