<div class="hero-section">
  <%= image_tag "image_638eb4.png", class: "img-4k" %>
  <div class="hero-overlay">
    <h1 class="fw-bold"><%= @gossip.title %></h1>
  </div>
</div>

<div class="container mt-5">
  <div class="card shadow-sm border-0">
    <div class="card-body p-4">
      <h2 class="card-title fw-bold text-capitalize"><%= @gossip.title %></h2>
      <p class="text-muted">Auteur : <%= @gossip.user.first_name %></p>
      <hr>
      <p class="fs-5"><%= @gossip.content %></p>
    </div>
  </div>
</div>